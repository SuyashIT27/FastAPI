#SQLite is a self-contained, serverless, zero-configuration, transactional SQL database engine.
import sqlite3
connect=sqlite3.connect("test.db",check_same_thread=Fasle)
cursor=connect.cursor()
cursor.execute("""
"""

)